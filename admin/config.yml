# =========================================================
# Configuration de l'administration (Decap CMS) — GuillaumEvent
# Voir SETUP-ADMIN.md pour la mise en place complète.
# =========================================================

backend:
  name: github
  repo: guillaume-KLN/GuillaumEvent   # ton dépôt GitHub
  branch: main
  # base_url = adresse de ton site en ligne (sert à l'authentification)
  base_url: https://dj-gv.netlify.app
  auth_endpoint: /.netlify/functions/auth

# Langue de l'interface d'admin
locale: "fr"

# Dossier où sont enregistrées les photos uploadées
media_folder: "assets/photos"
public_folder: "/assets/photos"

# Message de commit lisible quand tu ajoutes/modifies une photo
slug:
  encoding: "ascii"
  clean_accents: true

collections:
  - name: "galerie"
    label: "Galerie photos"
    description: "Ajoute, supprime ou réordonne les photos de la page Photos."
    files:
      - name: "photos"
        label: "Photos de la galerie"
        file: "data/gallery.json"
        format: "json"
        fields:
          - name: "photos"
            label: "Photos"
            label_singular: "Photo"
            widget: "list"
            summary: "{{fields.caption}}"
            fields:
              - { name: "image", label: "Image", widget: "image" }
              - { name: "caption", label: "Légende (optionnel)", widget: "string", required: false }
              - name: "size"
                label: "Taille de la vignette"
                widget: "select"
                default: "normal"
                options:
                  - { label: "Normale", value: "normal" }
                  - { label: "Large (2 colonnes)", value: "wide" }
                  - { label: "Haute (2 rangées)", value: "tall" }

  - name: "visuels"
    label: "Visuels du site"
    description: "Ta photo de présentation et le visuel du matériel/setup."
    files:
      - name: "site"
        label: "Photos principales"
        file: "data/site.json"
        format: "json"
        fields:
          - { name: "portrait", label: "Photo de présentation (Qui suis-je)", widget: "image", required: false }
          - { name: "setup", label: "Visuel matériel / setup (Équipement)", widget: "image", required: false }

  - name: "partenaires"
    label: "Partenaires"
    description: "Logos des partenaires, une liste par type de prestataire."
    files:
      - name: "partners"
        label: "Partenaires par catégorie"
        file: "data/partners.json"
        format: "json"
        fields:
          - name: "planners"
            label: "Wedding planners"
            label_singular: "Wedding planner"
            widget: "list"
            required: false
            summary: "{{fields.name}}"
            fields:
              - { name: "name", label: "Nom", widget: "string" }
              - { name: "logo", label: "Logo", widget: "image", required: false }
          - name: "lieux"
            label: "Lieux de réception"
            label_singular: "Lieu"
            widget: "list"
            required: false
            summary: "{{fields.name}}"
            fields:
              - { name: "name", label: "Nom", widget: "string" }
              - { name: "logo", label: "Logo", widget: "image", required: false }
          - name: "traiteurs"
            label: "Traiteurs"
            label_singular: "Traiteur"
            widget: "list"
            required: false
            summary: "{{fields.name}}"
            fields:
              - { name: "name", label: "Nom", widget: "string" }
              - { name: "logo", label: "Logo", widget: "image", required: false }
          - name: "photographes"
            label: "Photographes & vidéastes"
            label_singular: "Photographe"
            widget: "list"
            required: false
            summary: "{{fields.name}}"
            fields:
              - { name: "name", label: "Nom", widget: "string" }
              - { name: "logo", label: "Logo", widget: "image", required: false }
